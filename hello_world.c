#include <stdio.h>

int main() {                   //introduces "main", which has no arguments. uses { to start program.
    printf("Hello, world!\n"); // printf tells to print, words inside quotations tell what to print
}                              // closing bracket to end code
